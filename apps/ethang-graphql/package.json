{
  "browserslist": [
    "extends browserslist-config-baseline",
    "current node"
  ],
  "dependencies": {
    "@apollo/gateway": "^2.14.1",
    "@apollo/server": "^5.5.1",
    "@as-integrations/cloudflare-workers": "^1.1.1",
    "@cloudflare/workers-types": "^4.20260619.1",
    "dataloader": "^2.2.3",
    "graphql": "^17.0.1",
    "graphql-tag": "^2.12.7"
  },
  "devDependencies": {
    "@ethang/eslint-config": "^25.22.0",
    "browserslist-config-baseline": "^0.5.0",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.103.0"
  },
  "engines": {
    "node": ">=24"
  },
  "name": "ethang-graphql",
  "private": true,
  "scripts": {
    "build": "rover supergraph compose --elv2-license=accept --config supergraph.yaml > public/supergraph.graphql",
    "cf-typegen": "wrangler types",
    "deploy": "pnpm build && wrangler deploy",
    "dev": "pnpm build && wrangler dev",
    "lint": "eslint . --fix && pnpm tsc --noEmit",
    "start": "pnpm dev"
  },
  "type": "module",
  "version": "0.0.0"
}
