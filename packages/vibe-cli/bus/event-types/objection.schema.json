{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["reason"],
  "properties": {
    "reason":   { "type": "string" },
    "severity": { "type": "string", "enum": ["BLOCKER", "MAJOR", "HIGH", "MINOR"] }
  }
}
