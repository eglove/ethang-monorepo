# Unified Debate Session

**Date:** 2026-04-17
**Rounds:** 10
**Result:** PARTIAL_CONSENSUS

## Round 1

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-bdd, expert-tla, expert-tdd, expert-ddd, expert-edge-cases, expert-continuous-delivery

### Objections
- [tla] CORRECTNESS DEFECT (unresolved from session 4): GraphWriteFail markdownState formula `IF markdownState = "current" THEN "stale" ELSE "none"` transitions stale→"none" on a second write failure. Correct formula: `ELSE markdownState`. No invariant in S1–S27 catches this defect. Not in Revision 10 change log.
- [tla] S17 (HookErrorAllowsCompletion) remains nearly vacuous: consequent includes `pipelineState = "running"`, making the invariant trivially true in the transient state before PipelineComplete fires. The claim that hookState="error" alone cannot halt the pipeline is not formally encoded. Recommended replacement not applied.
- [tla] GraphNodesMonotone and GraphEdgesMonotone invariants are absent. Multi-agent graph accumulation — relied on by BDD integration scenarios (e.g., lines 1039–1045) — is structurally sound but formally unverified. A refactoring that incorrectly clears graphNodes at an epoch boundary would pass TLC.
- [tla] S27 (PendingCoherence) retrying-state coherence gap: PendingCoherence does not assert `graphState = "retrying" => pendingKind ∈ {"node","edge"}`. A refactoring entering retrying with pendingKind="none" satisfies all three S27 conjuncts vacuously, passes all 27 invariants, and leaves both retry-success actions permanently disabled.
- [tla] Missing liveness property for invoking→done: L2 (ValidRoleLeadsToModel) consequent is `resolvedModel /= NULL` (set by ResolveModel), not `routingState = "done"` (set by InvokeClaude). No liveness property covers the invoking→done transition. Recommended InvokingLeadsToDone property not added.
- [tla] D8 comment is misleading: states WF "ensures TLC must exercise hook intercept paths in liveness verification" but PipelineComplete accepts hookState="idle", so TLC L1 witnesses can complete without any hook intercept firing. WF is trivially discharged once pipelineState is terminal. Comment not corrected.
- [tla] Missing ASSUME RoleValueNormalizationIsDeclaredAbstraction: DocWriter and CodeWriter (TLA+ model values) silently normalize "doc-writer" and "code-writer" (BDD hyphenated strings). This is the only abstraction boundary in the spec not covered by an ASSUME, inconsistent with the D2/D7/O5/D9/D10/D11/D12/D14 pattern.
- [tla] Missing CloseHookPrecedesNextEpoch named safety invariant: the BDD guarantee "next stage MUST NOT begin before close hook completes" is structurally enforced but has no named TLA+ property. A refactoring moving agentsCompleted' increment into GraphWriteMarkdown instead of GraphWriteSuccess would pass all 27 invariants while violating this contract.
- [tla] HookErrorPreservesKind companion invariant absent: D15 preserved hookKind in hookState="error" for attribution, but no invariant asserts `hookState = "error" => hookKind ∈ {"es","rg"}`. TypeOK permits hookKind="none" in error state, which would defeat the attribution purpose of D15. A regression clearing hookKind on failure would pass TLC.
- [tla] graphOpsCount exclusion from retry budget undeclared: retry actions (GraphRetry, GraphRetryNodeSuccess, etc.) do not increment graphOpsCount, so an epoch can execute MaxGraphOps add-ops plus unlimited retry cycles. No ASSUME or invariant documents this deliberate exclusion, inconsistent with the established ASSUME documentation pattern.
- [tla] Missing CrashFreeExecutionIsDeclaredAbstraction ASSUME: agentsCompleted has no durability guarantee across a pipeline runner crash (Init resets it to 0). Unlike PreOutputCrashAndTimeoutAreOutOfScope (D11), no ASSUME declares the crash-free execution assumption that underpins agentsCompleted correctness.
- [tla] Inter-hook mutual exclusion has no named safety property: HookInterceptEs and HookInterceptRg both require hookState="idle", making simultaneous activation structurally impossible — but this guarantee is implicit. No named invariant makes it auditable. A future spec modification adding a second intercept action could introduce simultaneous activation undetected.
- [tla] GraphHaltCleanup markdownState compensation invariant absent: ASSUME D12 claims halt-before-write and write-failure share observable terminal behavior, but markdownState differs (none vs. stale), producing distinct operational outcomes for the next agent. No invariant asserts that GraphHaltCleanup from graphState="idle" leaves markdownState unchanged.
- [bdd] graphState=warn glossary entry (lines 26–31) defines warn exclusively as the outcome of GraphWriteFail (write-failure), omitting GraphHaltCleanup (pipeline-abort before any write) as a second production site. ASSUME D12 formally documents the conflation, but the BDD glossary contradicts it — a ubiquitous language violation.
- [bdd] Duplicate force-dedup scenarios at lines 635 and 646 describe the same causal arc (retryCount=3, fourth failing build, force-dedup fires) with identical preconditions and identical Thens. One must be removed or both must be clearly differentiated.
- [bdd] Missing simultaneous-degraded-terminal E2E scenario: no scenario covers hookState=error ∧ graphState=warn ∧ pipelineState=done. This joint terminal state is accepted by PipelineComplete's guard and is the most operationally likely combined degradation path. The BDD covers each failure mode in isolation only.
- [bdd] Pre-output hook crash failure mode (hook script exits non-zero before producing output) appears in two explicit scenarios but has no canonical glossary name. hookState=error is distinct from this path (hookRewritten=FALSE here). Practitioners discussing this failure mode have no shared term.
- [bdd] none→none→current recovery arc is absent. Two consecutive close-hook write failures both leave markdownState="none" (no CLAUDE.md ever written). No scenario covers this path across two agent epochs followed by a successful third write. Qualitatively distinct from the stale→current recovery arc that is covered.
- [bdd] Hook deployment partial-state not covered: no scenario tests settings.json containing a valid PreToolUse entry for es-hook.ps1 while the script file is absent (or vice versa). These are distinct, likely real deployment failure modes where the hook silently never fires or fails at invocation.
- [bdd] Dedup retry scenarios are not independently runnable: Given clauses like "the graph build failed once (retryCount=1)" encode accumulated state from a prior scenario, not a factory step. The full retryCount 0→1→2→3→force-dedup chain requires four tests runnable only in sequence. Fix: Scenario Outline with Examples or explicit factory steps.
- [bdd] 14 typed-wrapper scenarios are pure unit tests of a TypeScript class boundary (throw/no-throw for valid/invalid node and edge types) with no pipeline involvement. They belong in packages/vibe-cli/graph/wrapper.test.ts. Retain at most 2 BDD scenarios for integration-level confirmation.
- [bdd] No BDD scenario covers the GraphRetryNodeStillDuplicate/GraphRetryEdgeStillDuplicate path: agent substitutes a different path on retry and that replacement is also a duplicate, re-entering dedup_error. This path is explicitly modeled in TLA+ but has no BDD integration coverage.
- [bdd] Close-hook sequencing contract appears only in the glossary definition. The ordering rule — next stage MUST NOT begin before close hook completes — has no named BDD scenario or aggregate invariant asserting the intermediate ordering state.
- [bdd] Post-force-dedup edge validity has no scenario: after force-dedup keeps the first node registration and drops later duplicates, no scenario asserts that edges previously registered against the kept node remain structurally valid.
- [bdd] graphOpsCount=MaxGraphOps exact boundary has no scenario: no test drives graphOpsCount to exactly MaxGraphOps and attempts one additional add-operation. The boundary behavior (block, drop, epoch reset, error) is unspecified and untested.
- [bdd] Triple-failure path (routingState=error ∧ hookState=error ∧ graphState=warn ∧ pipelineState=halted) is uncovered. The "Routing failure halts the pipeline" integration scenario explicitly assumes "no hook interception occurs," eliding this concurrent failure combination.
- [bdd] settings.json schema structure validation is absent: no scenario validates that hook block entries have correct matcher field values, case-sensitive tool-name targets, or syntactically valid script paths. A deployment can satisfy all five registration scenarios with a silently inert hook.

## Round 2

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-tla, expert-bdd, expert-tdd, expert-ddd, expert-edge-cases, expert-continuous-delivery

### Objections
- [tla] GraphRetryNodeStillDuplicate and GraphRetryEdgeStillDuplicate leave retryCount unchanged when re-entering dedup_error for a substitute duplicate path. BDD implies fresh-cycle semantics (retryCount=0 at dedup_error entry for a new path); TLA+ inherits the prior count. No ASSUME declares this divergence. Confirmed independently by all 6 experts.
- [tla] No box-action temporal property preventing markdownState regression from 'stale' or 'current' to 'none'. The R11 bug fix (GraphWriteFail formula correction) is verified by inspection only — not by TLC. S30/S31 provide equivalent coverage for graphNodes/graphEdges but markdownState has no named monotonicity property.
- [tla] S33 (GraphHaltCleanupPreservesMarkdown) only verifies graphState='idle' entry case. D12 claims markdownState UNCHANGED for all 7 active GraphHaltCleanup entry states (idle, collecting, building, dedup_error, retrying, force_dedup, writing). Six entry states — including the most critical 'writing' case — are unverified by any named property.
- [tla] MaxGraphOps specification conflict: TLA+'s boundary silently disables GraphAddNode and GraphAddEdge when graphOpsCount=MaxGraphOps (no notification action exists). BDD's 'graphOpsCount at MaxGraphOps boundary' scenario asserts 'the system does not silently drop the add-operation without notifying the agent.' These are contradictory specifications that cannot both be correct.
- [tla] L4b (DuplicateEventuallyResolvedFully) comment claims to 'close the full chain end-to-end' but its antecedent requires pipelineState='running'. The halted-during-dedup_error path is covered by L4 via GraphHaltCleanup, not by L4b. The comment is factually inaccurate and a maintenance hazard.
- [tla] WF for HookInterceptEs and HookInterceptRg is included in Fairness without a corresponding liveness property that would exercise them, and without a named ASSUME registration. D8 acknowledges the limitation in a comment but does not follow the ASSUME documentation pattern established for D2–D18.
- [tla] GraphDuplicateDetected is asymmetrically named relative to its paired action GraphEdgeDuplicateDetected. All other paired node/edge actions use the Graph{Node,Edge}* convention. Should be renamed GraphNodeDuplicateDetected for consistency.
- [tla] PipelineComplete accepting hookState='idle' (allowing pipeline completion with no hook ever firing) is not registered as a named ASSUME, inconsistent with the D2–D18 documentation pattern for all other known divergences from BDD intent.
- [tla] GraphRetryNodeSuccess selects from NodePaths \ graphNodes but does not exclude pendingNode. Since pendingNode is not yet in graphNodes (it is the pending item, not accepted), it satisfies the guard and can be selected as its own substitute — creating a non-productive retry cycle that exhausts retryCount without making progress toward accepting a new node.
- [bdd] GraphRetryNodeStillDuplicate and GraphRetryEdgeStillDuplicate scenarios do not assert the retryCount value for the new duplicate path at dedup_error re-entry. TLA+ inherits the prior count; BDD glossary implies fresh cycle (retryCount=0 at dedup_error entry). Confirmed by 4 experts.
- [bdd] MaxGraphOps boundary scenario Then steps are untestable ('internally consistent' is undefined; 'if rejected or triggers an epoch reset' is conditional and not independently verifiable) and contradict TLA+'s actual behavior (silent disable of add-ops with no notification). The BDD glossary also marks the behavior as TBD despite the TLA+ spec defining it.
- [bdd] E2E triple-failure Given steps use multi-line prose with embedded parenthetical narratives that are not independently runnable and cannot be matched by a step definition regex. Should use a factory Given step (e.g., 'a pipeline run seeded with hookState=error and graphState=warn') consistent with the dedup factory step pattern.
- [bdd] No rg-hook deployment partial-state scenarios exist (entry-exists/script-absent; script-exists/entry-absent; incorrect matcher; wrong-case tool-name; invalid script path). This coverage exists only for es-hook, creating an asymmetric deployment safety gap.
- [bdd] hookPreOutputCrash scenarios (es-hook and rg-hook variants) have no Then clause specifying pipeline state after the crash — does the pipeline continue or halt? The behavioral postcondition is unspecified.
- [bdd] Hook registration order and inter-hook evaluation contract are unspecified: which hook evaluates first in settings.json? After the first hook rewrites the command, does the second hook receive the original command or the rewritten command? This is a missing contract for the sequential evaluation model.
- [bdd] E2E triple-failure Then clauses reference 'pipeline run state' as a persistent record of subsystem outcomes — an undefined domain concept with no glossary entry, no TLA+ variable, and no observable mapping. Step definitions cannot implement this assertion without a concrete definition.
- [bdd] No BDD scenario for a mapping config entry that exists and parses correctly but resolves to a model string not in {opus, sonnet, haiku} (e.g., 'gpt-4'). Also no scenario for an explicit Model override parameter containing an invalid value. These are distinct failure modes from missing/malformed config.
- [bdd] Force-dedup scenarios use 'removed from the graph' language, but TLA+ GraphForceDedup sets pendingNode/Edge to NULL — the duplicate was never accepted into graphNodes/graphEdges. The language incorrectly implies a post-hoc extraction from accepted state.
- [bdd] wrapper.test.ts delegation is unquantified: no file path link, no enumeration of the 12 scenarios by name, no minimum coverage assertion. The delegation creates an unverifiable coverage obligation.
- [bdd] No BDD scenario for stale→stale→current arc: two consecutive write failures where a prior successful CLAUDE.md exists (markdownState persists as 'stale' through both failures). The R11 TLA+ bug fix for the stale→none regression has no corresponding BDD regression test.
- [bdd] No atomic write temp-file rename failure scenario. This is distinct from crash-during-write: the temp file is written successfully but the rename step fails (e.g., target locked). Different observable state (temp file on disk, prior CLAUDE.md intact) and different operator recovery action.
- [bdd] tsx hang/timeout failure mode has no ubiquitous language name in the glossary. hookPreOutputCrash is a named canonical term; the tsx timeout is described inline only. Consistency requires a glossary entry (e.g., closeHookTimeout).
- [bdd] Force-dedup [WARN] logging assertions have no TLA+ backing and are not declared as integration-test-only, unlike the explicit callouts for hookPreOutputCrash and tsx hang. The asymmetric treatment will mislead unit test authors.
- [bdd] 'Agent fixes duplicate edge on first retry — build succeeds' scenario has no Then clause asserting retryCount=0 after the successful fix. TLA+ GraphEdgeBuildSuccess sets retryCount'=0; this reset is unverified by any BDD scenario.
- [bdd] Factory step 'seeded at force-dedup trigger state' used in the force-dedup edge scenario is not defined in the glossary. It is ambiguous whether this means graphState='dedup_error' ∧ retryCount=MaxRetries (force-dedup not yet fired) or graphState='force_dedup' (already fired). The step cannot be deterministically implemented.
- [bdd] No BDD scenario for a command ambiguous between file-search and text-search classification (e.g., find piped to grep). The Hook Ordering feature asserts mutual exclusion for unambiguous commands but does not specify the resolution rule when a command could match both hook patterns.
- [bdd] Bare filenames in CLAUDE.md output scenarios ('vibe.ps1', 'invoke-claude.ps1', 'state-repository.psd1') violate the node identity rule defined in the glossary (full path only). Step definitions matching on bare names will pass against implementations that do not enforce full-path identity.
- [bdd] Epoch-boundary reset scenario conflates two distinct reset mechanisms: force-dedup already resets retryCount via GraphAfterForceDedup before the epoch ends; GraphWriteSuccess/GraphWriteFail then reset it again at epoch close. The scenario seeds retryCount=3 from force-dedup but tests the epoch-end reset, conflating different TLA+ code paths with different guards.
- [bdd] No BDD scenario for an explicit Model override parameter value outside the valid model enum (e.g., Invoke-Claude -Role reviewer -Model 'claude-opus-99'). TLA+ ResolveModel guards inputModel ∈ Models before using it; behavior for an invalid explicit override is unspecified in BDD.

## Round 3

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-bdd, expert-tla, expert-tdd, expert-ddd, expert-edge-cases

### Objections
- [bdd] No BDD scenario covers the path where hookState='idle' at PipelineComplete (pipeline with no search commands completes normally) — ASSUME D22 explicitly declares this a BDD coverage gap but was not closed when D22 was added in Revision 12.
- [bdd] No BDD scenario exercises GraphHaltCleanup from graphState='writing' — TLA+ S33 was generalized in Revision 12 to cover this entry state as the most operationally significant case (distinguishing abort-during-write from GraphWriteFail), but there is no corresponding acceptance-test scenario.
- [bdd] 'epoch' is used in scenario titles, step text, and the retryCount glossary entry but has no glossary definition of its own, leaving the domain concept (a MaxGraphOps-bounded collection phase ending with a close-hook write) undefined and its equivalence to 'agent session' unstated.
- [bdd] 'MaxAgents' has no glossary entry despite MaxRetries and MaxGraphOps both having explicit glossary definitions — the pipeline termination condition (all agents complete their close hooks) is left without a cardinality anchor in the BDD ubiquitous language.
- [bdd] The glossary never states that one agent session constitutes exactly one epoch, leaving the two reset sites for retryCount ('intra-epoch' via GraphAfterForceDedup and 'epoch-end' via GraphWriteSuccess/GraphWriteFail) ambiguous as to whether session-end and epoch-end are the same event.
- [bdd] The 'Agent re-submits duplicate without fix' Scenario Outline has rows {before:1, after:2} and {before:2, after:3} but no row for {before:0, after:1}, leaving the first GraphRetry firing — where retryCount increments from 0 to 1 on first re-submission — without an independently runnable scenario.
- [bdd] There is no BDD scenario for 'explicit model override to the same value the mapping would produce' (e.g., Role 'reviewer' with Model 'haiku' when the mapping resolves reviewer→haiku), leaving the override identity-equality path in ResolveModel untested.
- [tla] No ASSUME documents that AllModels = Models ∪ {NULL} excludes invalid model strings — BDD's two halt-on-invalid-model scenarios ('Config mapping resolves to out-of-enum model' and 'Explicit Model override with invalid value') have no TLA+ counterpart and violate the ASSUME discipline established by D2–D22.
- [tla] No ASSUME documents that StartPipeline always assigns inputRole from AllRoles, making the NULL-inputRole path structurally unreachable — BDD's 'Role parameter is required' halt scenario has no TLA+ counterpart and this gap is undocumented.
- [tla] S27 (PendingCoherence) asserts 'graphState = retrying => pendingKind ∈ {node, edge}' but omits the symmetric conjunct for 'graphState = building' — a refactoring entering building with pendingKind=none would disable all four exit actions (GraphBuildSuccess, GraphEdgeBuildSuccess, GraphNodeDuplicateDetected, GraphEdgeDuplicateDetected) creating a silent deadlock that passes all S1–S34 invariants.

## Round 4

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-bdd, expert-tla, expert-edge-cases, expert-ddd, expert-tdd

### Objections
- [tla] GraphRetryEdgeSuccess self-substitution bug: pendingEdge satisfies <<from,to>> not in graphEdges (it is not yet committed), so the action can select pendingEdge as its own substitute producing a non-productive retry cycle. Symmetric to R12 node fix. Add guard: <<from,to>> /= pendingEdge.
- [tla] Missing ASSUME D25 for external pipeline abort: BDD B2 describes pipelineState transitioning to halted from an external abort signal. In TLA+ only ValidateRoleFail and MappingLookupFail set pipelineState=halted, so S6 HaltedMeansRoutingError implicitly constrains all halts to routing-originated. Undeclared structural assumption.
- [tla] Missing ASSUME D26 for two-phase atomic write collapse: BDD defines a temp-file to rename mechanism with a distinct rename-failure scenario. TLA+ collapses both phases to a single GraphWriteMarkdown transition with no rename-failure counterpart action.
- [tla] Missing liveness: routingState=resolving leads-to routingState in {invoking,error}. A deadlock in resolving state violates no named liveness property. Analogous gap to L8 which closed the invoking-to-done chain.
- [tla] Missing liveness: graphState=writing leads-to graphState in {done,warn,collecting}. WF fairness on GraphWriteSuccess and GraphWriteFail provides progress but no named liveness property captures this transition.
- [tla] GraphHaltCleanup resets retryCount to 0 but leaves graphOpsCount UNCHANGED, asymmetric with GraphWriteSuccess and GraphWriteFail which reset both. Asymmetry is undocumented. No invariant asserts graphState in {done,warn} implies graphOpsCount=0.
- [tla] GraphBuildSuccess should be renamed GraphNodeBuildSuccess. R12 established the Graph{Node,Edge}* naming convention but did not rename this action. BDD cross-references already use GraphNodeBuildSuccess, creating a cross-document naming inconsistency.
- [tla] HookRewrite, HookExecuteSuccess, and HookExecuteFail lack pipelineState=running guard. A concurrent ValidateRoleFail can set pipelineState=halted while hookState=intercepting, allowing hook actions to complete after pipeline halt. This design choice is undeclared as ASSUME or comment.
- [bdd] Agent fixes duplicate edge is a single flat scenario seeded at retryCount=1 only. The parallel node-fix path is a Scenario Outline with rows for count=1, 2, and 3. GraphEdgeBuildSuccess at retryCount=2 and retryCount=3 are untested. Promote to Scenario Outline.
- [bdd] Node-fix Scenario Outline missing retryCount=0 row: no scenario where an agent enters dedup_error and removes the duplicate before any GraphRetry fires. GraphNodeBuildSuccess can fire from retryCount=0 and this fix-before-retry path is untested.
- [bdd] Missing Scenario Outline: Agent re-submits duplicate edge without fix with retryCount incrementing from N to N+1. The parallel of the node re-submit outline (rows 0 to 1, 1 to 2, 2 to 3) is entirely absent for edges.
- [bdd] StillDuplicate scenarios assert retryCount=0 for the substitute path without a D19 integration test note. D19 explicitly documents TLA+ contradicts this (shared budget not per-cycle reset). Add a note matching the D11 hookPreOutputCrash pattern.
- [bdd] B7 identity-override Then clause asserting the override was applied via the explicit Model parameter path is not a state-observable assertion. No TLA+ variable distinguishes the two code paths. Either add a concrete observable discriminator or restrict to a behavioral assertion and add an ASSUME for the coverage gap.
- [bdd] Missing glossary entries for routingState (used in scenario steps throughout without definition), pipelineState (primary aggregate-level observable with no definition), and agentsCompleted (cardinality anchor for pipeline termination with no definition).
- [bdd] hookPreOutputCrash and closeHookTimeout scenarios do not explicitly assert markdownState, unlike crash-during-write and rename-failure scenarios which do. These out-of-TLA+ scenarios are the only formal statement of expected markdownState behavior for those failure modes.
- [bdd] No BDD scenario for pipeline abort during graphState=collecting. B2 covers the writing entry state for GraphHaltCleanup. S33 was generalized to all 7 active entry states. Collecting is the most operationally frequent abort state and has no dedicated markdownState-UNCHANGED scenario.
- [bdd] D3 multi-command sequential scenarios and D21 liveness-limitation scenarios carry no ASSUME cross-reference notes. D22 was explicitly closed by B1 with a note referencing D22. D3 and D21 integration test obligations should receive the same treatment.
- [bdd] retryCount resets when session ends scenario seeds retryCount=2 mid-dedup-cycle but GraphWriteMarkdown requires graphState=collecting. Dedup resolution steps are elided. Use the factory pattern seeded at graphState=collecting and retryCount=2.
- [bdd] The retryCount=3=MaxRetries And step in the force-dedup boundary scenario is commentary masquerading as a Gherkin step. It establishes no new state and duplicates the factory Given. Move to a hash comment block above the scenario.

## Round 5

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-bdd, expert-tla, expert-tdd, expert-edge-cases, expert-ddd

### Objections
- [bdd] System prompt feature (lines 1562–1600): 6 structurally identical scenarios varying only by role name — consolidate into a Scenario Outline with a 6-row Examples table
- [bdd] hookPreOutputCrash Then steps (lines 424–435 es-hook, 511–520 rg-hook) do not assert hookState; since hookRewritten=FALSE, hookState cannot reach 'error' (S3), and the scenario should assert this distinction explicitly
- [bdd] No negative scenario for wrong hook registration order: when rg-hook appears before es-hook, file-search commands are silently rewritten to the wrong tool with no error or warning
- [bdd] D27 cross-document gap: TLA+ D27 declares hook actions can complete after pipelineState='halted', but no BDD scenario asserts this concurrent-halt observable path
- [bdd] 'Close hook fires exactly once' scenario (lines 1240–1248) references session table rows via prose strings, not table-column bindings — ambiguous for step definition authors
- [bdd] graphOpsCount epoch-end reset has no standalone scenario; only embedded as a secondary Then clause in the retryCount epoch-end reset scenario (line 1193)
- [bdd] Close-hook sequencing contract (line 1229) has a positive test only; no negative test specifies what observable constitutes a detectable violation
- [bdd] Final-agent close hook producing graphState='done' (TLA+ GraphWriteSuccess terminal branch: agentsCompleted+1 >= MaxAgents) has no dedicated BDD scenario
- [bdd] addEdge with graphNodes={} (entirely empty graph) boundary is untested; TLA+ GraphAddEdge is guarded on graphNodes /= {} but no BDD scenario covers this empty-graph boundary
- [bdd] No integration-test obligation note for routingState='resolving' intermediate: TLA+ L9 closes a deadlock gap BDD has no scenario for; test implementers need a note analogous to D21 pattern
- [bdd] Hook Registration Feature conflates behavioral domain invariants (ordering, sequential evaluation) with deployment infrastructure concerns (partial-state failures, schema misconfiguration) — bounded context leakage
- [bdd] Glossary 'build succeeds/fails' is ambiguous between graph state-machine resolution (TLA+-modeled) and tsx compilation outcome (out of TLA+ scope); a disambiguating entry is needed
- [bdd] No aggregate invariant scenario asserting markdownState=current implies agentsCompleted>=1 and the most recent close hook succeeded
- [bdd] Argument-translation contract ('equivalent arguments') is undefined; a hook that rewrites the command token but drops flags passes all BDD hook intercept scenarios; needs a note analogous to ASSUME D20
- [tla] Liveness gap: routingState='validating' has no named liveness property; add L11 ValidatingLeadsToResolvingOrError: (routingState='validating') ~> (routingState in {'resolving','error'})
- [tla] Safety gap: hookState='error' => hookCommand=NULL holds by action structure but has no named invariant; S15 asserts this only for hookState='done'; add HookErrorClearsCommand invariant
- [tla] S1 (UnknownRoleNeverInvokes) does not directly assert pipelineState='halted'; strengthen with a conjunct directly asserting pipelineState='halted' in the error terminal
- [tla] ASSUME gap: hook evaluation order (es-hook before rg-hook by array index) and sequential delegation chain have no TLA+ counterpart and no ASSUME declaration; add D28 under the D2-D27 discipline
- [tla] D7 does not cover error notification payload; BDD specifies the dedup_error message contains duplicate path and retryCount value, but TLA+ has no notification variable; add D7 extension or new ASSUME
- [tla] B7 identity-override code-path gap is only a Gherkin comment, not a formal TLA+ ASSUME; add a named propositional ASSUME (D29) in TokenSaving.tla under the established ASSUME discipline
- [tla] MaxAgents=1 model-checking scope undeclared: multi-epoch 'collecting' return branch of GraphWriteSuccess/GraphWriteFail never verified by TLC; S25/S30/S31 unverified in multi-agent scenarios
- [tla] GraphRetryEdgeSuccess single-node forced-exhaustion path: when |graphNodes|=1 and pendingEdge is the unique self-loop, R14 guard eliminates all candidates; an undeclared consequence with no BDD scenario or ASSUME
- [tla] CommandTokenRewriteOnly gap: HookRewrite models rewrite as a boolean flag only with no encoding of command token replacement; add CommandTokenRewriteOnlyIsDeclaredAbstraction ASSUME

## Round 6

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-bdd, expert-tla, expert-tdd, expert-edge-cases, expert-ddd

### Objections
- [bdd] B4: Factual cross-reference error in B8-14 glossary (line 329). Reads 'T8-9 candidate' but no such ASSUME exists; D33 was registered in TLA+ R15. Must read 'D33 — CommandTokenRewriteOnlyIsDeclaredAbstraction'. (3 experts blocking)
- [bdd] B5: agentsCompleted glossary 'regardless of success or failure' can be misread to include GraphHaltCleanup abort path, which does NOT increment agentsCompleted. Must add explicit exclusion. (2 experts blocking, 2 recommending)
- [bdd] B7: Missing scenario for GraphHaltCleanup when markdownState='current'. All existing abort scenarios seed markdownState at none/stale. The observable that markdownState stays 'current' on abort (vs GraphWriteFail which transitions current→stale) is unverified. (1 expert blocking)
- [bdd] B9: Missing scenario for multi-agent GraphWriteFail returning graphState to 'collecting'. All write-failure scenarios are ambiguous between final-agent (→warn) and mid-pipeline (→collecting) paths. D31 declares this branch TLC-unverified. (2 experts blocking)
- [tla] T6: D31 underspecifies unverified properties. Omits L4 and L6 from the named list. Provides no three-point entries (why unverified / boundary condition / compensating test) per property, leaving the risk register unauditable. (2 experts blocking)
- [tla] T1: Missing liveness property for graphState='building'. No named property asserts 'building ~> {collecting,dedup_error}', departing from the L8/L9/L10/L11 naming pattern for all other intermediate states. (1 expert blocking, 2 recommending)
- [tla] T2: S1/S2 invariant asymmetry. R15 added halt conjunct to S1 but not S2, despite MappingLookupFail atomically setting pipelineState='halted' identically to ValidateRoleFail. Requires symmetric fix or documented rationale. (1 expert blocking, 1 recommending)

## Round 7

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-tla, expert-bdd, expert-ddd, expert-tdd, expert-edge-cases, expert-continuous-delivery

### Objections
- [bdd] B-1: B9 (mid-pipeline GraphWriteFail ELSE branch) is missing a 'retryCount is reset to 0' Then assertion. TLA+ GraphWriteFail sets retryCount'=0 in BOTH the terminal IF branch and the mid-pipeline ELSE branch; the epoch-boundary retryCount reset is unverified for the ELSE case.
- [bdd] B-2: B8-9 (addEdge when graphNodes={}) contains a factual cross-reference error: the comment attributes the graphNodes /= {} guard to 'D14' (GraphWriteMarkdownRequiresCollecting). D14 addresses graphState guard sequencing for GraphWriteMarkdown, not graphNodes cardinality for GraphAddEdge. The correct reference is the O2 fix on GraphAddEdge (TLA+ ~line 1420).
- [bdd] B-3: Missing GraphWriteSuccess ELSE branch scenario — the symmetric partner to B9. With MaxAgents=3 and agentsCompleted=1, a successful close-hook write must return graphState to 'collecting' (not 'done'). This is the primary positive multi-agent scenario and is absent from BDD.
- [bdd] B-4: Missing dedicated multi-agent monotone scenario anchoring S30/S31 (GraphNodesMonotone, GraphEdgesMonotone). D31 lists compensating integration tests for these unverified properties, but no BDD scenario seeds a two-epoch pipeline and asserts that graphNodes/graphEdges from epoch 1 are still present after epoch 2.
- [bdd] B-5: Missing node identity bare-filename rejection scenario. The glossary declares 'Bare filenames without a directory prefix are NOT valid node identities.' No negative BDD test verifies that passing a bare filename (e.g., 'vibe.ps1') to .addNode() triggers a build error.
- [bdd] B-6: E2E triple-failure Then clauses embed non-Gherkin TLA+ state notation as parentheticals — e.g., 'Then the pipeline halts with a missing-config error (routingState=error, pipelineState=halted)'. Step definition authors cannot determine whether parentheticals are comments or automation obligations. These should be promoted to separate And steps.
- [tla] T-1: L12 property name 'BuildingLeadsToCollectingOrDedupError' is inconsistent with its R17-widened consequent, which now includes 'warn' as a valid exit state (via GraphHaltCleanup from graphState='building'). The name should be updated to reflect all consequent states, e.g., 'BuildingLeadsToCollectingOrDedupErrorOrWarn'.
- [tla] T-2: MaxRetries=1 in the .cfg file is an undeclared scope boundary analogous to D31 (MaxAgents=1). TLC verifies force-dedup semantics with a 2-failure budget only; BDD scenarios seeded at MaxRetries=3 are never exercised by TLC. A new ASSUME D34 should formally declare this scope limitation, identify which properties are affected (at minimum L4/L5's force-dedup arm), and list compensating integration tests.
- [tla] T-3: S32 (CloseHookPrecedesNextEpoch) expresses only the post-condition temporal operator: agentsCompleted increments only from graphState='writing'. The pre-condition contrapositive — graphState not in {writing} implies agentsCompleted is unchanged — is not a named, directly auditable safety invariant. A regression where a new action increments agentsCompleted outside the 'writing' state could satisfy S32 only via simultaneous state writes. An explicit named invariant should be added.

## Round 8

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-tla, expert-bdd, expert-tdd, expert-ddd, expert-edge-cases

### Objections
- [tla] Missing L13 (RetryingLeadsToBuildingOrDedupError): graphState='retrying' has no named liveness property, violating the one-named-property-per-non-terminal-state pattern established by L8–L12. All four WF-fair exit actions exist; only the property name is absent.
- [tla] S37 justification counterexample is not constructible from the actual action set: no action in Next simultaneously sets graphState'='writing' AND increments agentsCompleted' from graphState='collecting'. The comment overstates S37's regression-detection value and will mislead readers who attempt to construct the claimed counterexample.
- [tla] MaxAgents=0 structural deadlock not excluded by any ASSUME: GraphWriteMarkdown guard (agentsCompleted < MaxAgents) is permanently FALSE at MaxAgents=0; graphState can never reach 'done' or 'warn'; L5 is violated. Requires ASSUME MaxAgents >= 1 analogous to the implicit MaxRetries >= 1 requirement.
- [tla] pipelineState='idle' Init artifact has no ASSUME declaration, asymmetric with D24 (NULL inputRole Init artifact). pipelineState='idle' has no BDD counterpart; no scenario seeds or asserts it. Requires a mini-ASSUME or comment analogous to D24.
- [tla] hookKind is a named TLA+ state variable appearing in S28 (HookErrorPreservesKind) and D15, but has no corresponding BDD glossary entry. The domain concept 'which hook subsystem caused a failure' has clear behavioral significance for failure attribution diagnostics.
- [tla] No named invariant for graphState='writing' => agentsCompleted < MaxAgents. Structurally guaranteed by GraphWriteMarkdown's guard, but auditable only via cross-action reasoning. A named S38 would make this independently verifiable if the guard were ever refactored.
- [tla] S20 (RetryCountClean) excludes dedup_error without documenting that retryCount > 0 in dedup_error via GraphRetryNodeStillDuplicate/GraphRetryEdgeStillDuplicate is intentional per D19. A regression resetting retryCount to 0 on StillDuplicate re-entry would pass S20 silently.
- [bdd] Two hookPreOutputCrash And steps (es-hook ~line 598, rg-hook ~line 683) embed TLA+ parenthetical explanations after em-dashes in automatable step text. Same defect class as B-6. Step definition authors cannot determine where the assertion ends and the explanation begins.
- [bdd] Missing L11-pattern integration test obligation note for routingState='validating' intermediate state. The Model Routing Feature header documents the D21/L9 pattern for 'resolving' but has no analogous note for 'validating', leaving the L11 gap unanchored at the BDD level.
- [bdd] D19 StillDuplicate retryCount annotation creates unresolvable step-definition dual-truth: BDD Then clauses assert retryCount=0 for new duplicate path; TLA+ D19 model has retryCount UNCHANGED (inherited > 0 via shared budget). The annotation identifies the divergence but does not resolve which value step definitions must assert.
- [bdd] No BDD scenario anchors D31 S25 compensating test for the negative path: no scenario seeds agentsCompleted < MaxAgents and asserts pipelineState is NOT 'done'. Without this negative-path scenario, the S25 compensating integration test has no implementable target.
- [bdd] pipelineState glossary uses 'iff' for what TLA+ implements as a liveness property (SF-fair PipelineComplete). The biconditional implies pipeline completion is a safety invariant guaranteed the moment the conjunction holds. Correct framing: safety direction (done=>all terminal) is instantaneous; liveness direction (all terminal=>eventually done) requires SF fairness.
- [bdd] No BDD scenario covers MaxGraphOps=0 zero-budget epoch: every epoch produces an empty write; no node or edge is ever accepted. Silent misconfiguration with no BDD regression protection.
- [bdd] No BDD scenario covers MaxRetries=0 zero-retries instant force-dedup: first duplicate immediately triggers force-dedup with no agent notification. Silent misconfiguration with no BDD regression protection.
- [bdd] D18 (CrashFreeExecutionIsDeclaredAbstraction) compensating test obligation is declared at category level ('TypeScript/PowerShell process management') with no named crash+restart scenario. Requires a specific named integration test scenario: crash after epoch 1 close hook succeeds; verify CLAUDE.md from epoch 1 persists and the recovery contract is explicitly tested.
- [bdd] D32 (GraphRetryEdgeSingleNodeForcedExhaustionIsDeclaredAbstraction) has no BDD scenario for its observable: agent experience when the single-node self-loop forces exhaustion via StillDuplicate alone (D19 inherited-budget semantics throughout). TLC-verified at model level; BDD observable unanchored.
- [bdd] No BDD scenario covers MaxGraphOps boundary + active dedup cycle: dedup resolves successfully when graphOpsCount=MaxGraphOps, subsequent add-operations are silently disabled. Combined dual-silent-failure path with no BDD coverage.
- [bdd] D21 integration test anchor scenarios ('Hook rewrites find to es', 'Hook rewrites grep to rg') assert functional outcomes only — no Then step asserts a liveness bound that the hook fires within the same tool-call event and confirms completion before the tool call returns.
- [bdd] Factory Given steps ('a graph dedup cycle seeded with retryCount=N') have no state-assertion checkpoint before the When step. Silent seeding failure produces false-positive Scenario Outline rows with no detectable failure at the Given boundary.
- [bdd] D3 'Hook intercepts two sequential commands' scenarios use 'later in the same agent session' Given without specifying how step definitions must verify hookState is correctly reset between PreToolUse events. The integration test is the entire verification mechanism for D3; the reset observable must be specified.
- [bdd] Hook Deployment Infrastructure Feature: 10 scenarios are structurally 5 parallel pairs differing only by hook name (es/rg). Apply B8-1 Scenario Outline discipline: consolidate into 5 Scenario Outlines with a hook-name Examples table.
- [bdd] Sequencing invariant ('next pipeline stage MUST NOT begin before close hook completes') is declared in the Knowledge Graph Feature context but is a pipeline-orchestration invariant. TLA+ S32/S37 are its formal anchors. Add a cross-reference in the pipelineState glossary entry to prevent future developers from missing it.
- [bdd] The 'retrying' graph state has no BDD scenario anchoring the retrying->building transition (GraphRetryNodeSuccess/GraphRetryEdgeSuccess path). The path is TLA+-verified via D7 but has no integration test anchor observable at the BDD step-definition level.

## Round 9

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-tla, expert-bdd, expert-tdd, expert-edge-cases, expert-ddd

### Objections
- [bdd] S25 negative-path scenario (agentsCompleted=1, MaxAgents=3) lacks a D31 cross-reference and integration-test obligation annotation — the assertion is not backed by a TLA+ proof at MaxAgents=3; apply the L11 annotation pattern.
- [bdd] hookPreOutputCrash Then steps retain TLA+ parenthetical references in Gherkin comments — replace with plain-English prose or ASSUME declaration index cross-reference only.
- [bdd] hookKind {"none","es","rg"} has no BDD glossary entry despite being a domain concept with first-class behavioral consequences (S28 HookErrorPreservesKind, hook evaluation order, D15). The hook subsystem bounded context is incomplete without it.
- [bdd] pipelineState glossary lists only three values (running/done/halted); TLA+ Init emits a fourth value "idle" with no exclusion note in the BDD — add an explicit note that "idle" is a TLA+ initialization artifact not a domain-observable state, mirroring the D24 treatment for inputRole=NULL.
- [bdd] retryCount force-dedup boundary predicate is underspecified in the glossary — the exact comparison triggering force-dedup (>= MaxRetries vs. = MaxRetries) and the reset value at each of the three reset sites must be precisely stated.
- [bdd] WRONG hook order scenario asserts silent misbehavior but has no specified observable — silent failures are only automatable if a concrete log entry, output artifact, or return value is named; the scenario as written cannot be automated.
- [bdd] D21 liveness bound integration-test obligation lacks an observable signal specification — "hook completes and rewrite confirmed before tool call returns control to agent" provides no test-harness event constituting "control returned"; name the concrete observable (e.g., hookRewritten=TRUE in audit log before PostToolUse event fires).
- [bdd] Multi-dimension factory step slash-notation is ambiguous — "a pipeline run seeded with hookState=error / graphState=warn" is undefined Cucumber grammar; split into two explicit AND-chained Given steps with a mandatory state-assertion checkpoint.
- [bdd] MaxRetries=1 boundary scenario is absent — the off-by-one between zero retries and one retry is the most likely implementation error; add an explicit scenario: MaxRetries=1, first duplicate detected, force-dedup fires on the second failure.
- [bdd] rg-rewritten path token node validity is unspecified — the BDD must specify whether rg output tokens (e.g., "src/foo.ts:42:keyword") pass or fail the node identity validation rule; the bare-filename rule alone does not answer this.
- [bdd] GraphHaltCleanup halt-during-write standalone scenario is missing — graphState="writing" is the highest-consequence GraphHaltCleanup entry state and has no dedicated BDD scenario with explicit markdownState assertions.
- [bdd] Hook matching level is unspecified — no glossary rule states whether hooks match on surface token or on resolved cmdlet after alias expansion; every new PowerShell alias is a systematic coverage gap.
- [bdd] S38 WritingAgentsCompletedBound (new R19 invariant: graphState="writing" => agentsCompleted < MaxAgents) has no BDD scenario exercising the boundary condition (agentsCompleted=MaxAgents-1 entering writing state) — the invariant has no BDD-layer falsifiability test.
- [tla] hookState intermediate states ("intercepting", "rewriting") have no named liveness properties — the one-property-per-non-terminal-state pattern established for graphState (L12/L13) and routingState (L9/L11) is not applied to hookState.
- [tla] L4's force_dedup transient waypoint is not documented in the liveness chain — dedup_error can stutter at force_dedup without L4 being falsified if WF(GraphForceDedup) fairness is weakened; extend L4's comment chain to enumerate force_dedup as an explicitly covered transient waypoint.
- [tla] PipelineComplete guard / S6 sequencing during halt-cleanup is not confirmed — verify that PipelineComplete's guard explicitly requires graphState ∈ {"done","warn"} before it can fire during a halt-cleanup sequence, so S6 cannot be violated by a premature PipelineComplete while graphState is still transitioning to "warn".
- [tla] L9 (ResolvingLeadsToInvokingOrError) and L11 (ValidatingLeadsToResolvingOrError) TLC-verification status is inconsistent with the BDD's framing of them as integration-test obligations — if TLC-verified, label as "supplements TLC"; if not verified, add explicitly to the D31 scope limitations list.
- [tla] D26 (TwoPhaseWriteCollapseIsDeclaredAbstraction) scope applies only per-agent but inter-agent concurrent close hook races (two tsx processes both attempting atomic rename to CLAUDE.md) are unmodeled and undeclared; extend D26 scope or add a new ASSUME.
- [tla] FindCmds={"find"} and GrepCmds={"grep"} are intentional set collapses of the full BDD command enumeration ({find,ls,dir,gci,...} and {grep,egrep,fgrep,sls,...}) but lack a D-numbered decision documenting this collapse — analogous to D9 (PlainCmd normalization) and D16 (role normalization).
- [tla] S6 HaltedMeansRoutingError crosses the routing subsystem boundary into pipeline orchestration and its restricted scope (per D25: structural property of the model only) is documented in the attached ASSUME note but not annotated at the S6 definition itself — add explicit scope annotation in the spec body.
- [tla] D31 second .cfg configuration (MaxAgents=2, MaxGraphOps=3, MaxRetries=1) has not been added — at round 14, deferring S25/S30/S31/L4/L5 verification for multi-agent configurations represents the largest unmitigated risk; a minimal multi-agent model check is needed.
- [tla] GraphHaltCleanup markdownState freshness gap — markdownState="current" in TLA+ does not guarantee the CLAUDE.md reflects the current epoch (a halt after epoch 1 leaves "current" from epoch 1 data); add a GraphMarkdownFreshness property or extend D12/D26 scope documentation.
- [tla] D27 integration test obligation (hook actions completing after pipeline halt) lacks a specified injection mechanism — two engineers will write incompatible tests without knowing whether to use a mock halt signal, artificial delay, or real runtime pause; specify the action interleaving sequence the test must simulate.

## Round 10

**Result:** PARTIAL_CONSENSUS
**Experts:** expert-bdd, expert-tla, expert-ddd, expert-tdd, expert-edge-cases

### Objections
- [bdd] hookKind glossary entry (OBJ-3, R17) states S28 as 'hookState=done IMPLIES hookKind ∈ {es,rg}' — this is factually inverted. TLA+ HookExecuteSuccess clears hookKind to 'none' at the done terminal. S28 covers hookState=error only: hookState=error => hookKind ∈ {es,rg}. Step definitions for the success path written against this glossary will assert the wrong postcondition. The glossary must be corrected and explicit hookKind=none assertions added to hookPreOutputCrash scenarios and the no-search-command scenario. (5/5 expert agreement)
- [bdd] No BDD scenario anchors the TokenSaving-MultiAgent.cfg (MaxAgents=2, MaxRetries=1) combined force-dedup + cross-epoch path. The multi-agent cfg was introduced by D31 R20 O-8 and exercises a path no existing scenario covers: agent-1 triggers force-dedup (two total failures exhaust MaxRetries=1), force-dedup resolves within epoch 1, agent-1 close hook succeeds, agent-2 epoch proceeds to graphState=done. The D31/D34 scope intersection is TLC-verified but has zero BDD coverage. (5/5 expert agreement)
- [bdd] D12 epoch-relative freshness gap (R20 O-9) declares a compensating integration test obligation but has no named BDD scenario. D12 specifies: MaxAgents=2, epoch-1 completes successfully (markdownState=current), epoch-2 begins collecting, GraphHaltCleanup fires (graphState→warn, markdownState UNCHANGED=current). The BDD Then clause must assert CLAUDE.md reflects only epoch-1 data by file content inspection — not by relying on markdownState alone. No existing scenario covers this multi-epoch freshness path. (4/5 expert agreement)
- [tla] L13 property name 'RetryingLeadsToBuildingOrDedupError' omits 'warn' from its consequent. The formal body is (graphState = retrying) ~> (graphState ∈ {building, dedup_error, warn}) — the warn consequent (via GraphHaltCleanup from retrying) is present in the body but absent from the name. This is the identical defect class as the Revision 18 T-1 blocking fix that renamed L12 from BuildingLeadsToCollectingOrDedupError to BuildingLeadsToCollectingOrDedupErrorOrWarn. Must rename to RetryingLeadsToBuildingOrDedupErrorOrWarn and update .cfg PROPERTY entry, liveness chain comment table, and all cross-references. (5/5 expert agreement)
- [tla] No named invariant asserts hookState=rewriting => hookKind ∈ {es,rg}. The hookKind coherence chain has named properties for intercepting (S13/S14), error (S28), and done (S15) but the rewriting state is unguarded. A refactoring that entered rewriting with hookKind=none (bypassing HookInterceptEs/Rg) would pass all existing invariants silently — S13/S14 fire only in intercepting, S28 in error, S15 in done. Add as a named invariant (candidate S39) to close the rewriting-state gap. (4/5 expert agreement; expert-tla and expert-bdd escalate to blocking)


