# Claude Warden - Project Configuration
# Allows PowerShell and Pester test execution alongside standard dev tools

defaultDecision: allow
askOnSubshell: true
notifyOnAsk: true
notifyOnDeny: true

alwaysAllow:
  - pwsh
  - powershell
  - Invoke-Pester
  - python3
  - pnpm
